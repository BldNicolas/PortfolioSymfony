{% extends 'base.html.twig' %}

{% block title %}AboutCustomSection{% endblock %}

{% block body %}
    <h1>AboutCustomSection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about_custom_section.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ about_custom_section.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ about_custom_section.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_about_custom_section_index') }}">back to list</a>

    <a href="{{ path('app_about_custom_section_edit', {'id': about_custom_section.id}) }}">edit</a>

    {{ include('about_custom_section/_delete_form.html.twig') }}
{% endblock %}
