<form method="post" action="{{ path('app_profile_section_delete', {'id': profile_section.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profile_section.id) }}">
    <button class="btn">Delete</button>
</form>
